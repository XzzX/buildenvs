FROM ubuntu:24.04

ENV DEBIAN_FRONTEND=noninteractive

COPY *.sh .
RUN ./before_compiler_setup.sh

RUN apt-get update \
 && apt-get install -y \
        clang \
        clang-format \
        clang-tidy

ENV CC=clang
ENV CXX=clang++

RUN ./after_compiler_setup.sh
